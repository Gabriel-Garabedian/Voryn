Estes arquivos foram consolidados em `../schema.sql` e não devem mais ser
executados — continham tabelas, índices e policies duplicadas, além de
referências a colunas inexistentes. Mantidos aqui só como histórico.
